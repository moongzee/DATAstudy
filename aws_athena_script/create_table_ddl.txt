CREATE EXTERNAL TABLE IF NOT EXISTS `test`.`TripData1` (
  `VendorID` int,
  `Ipep_pickup_datetime` string,
  `Ipep_dropoff_datetime` string,
  `store_and_fwd_flag` string,
  `RatecodeID` int,
  `PULocationID` int,
  `DOLocationID` int,
  `passenger_count` int,
  `trip_distance` float,
  `fare_amount` float,
  `extra` float,
  `mta_tax` float,
  `tip_amount` float,
  `tolls_amount` float,
  `ehail_fee` float,
  `improvement_surcharge` float,
  `total_amount` float,
  `payment_type` int,
  `tirp_type` int
)
-- csv 파일 읽기
ROW FORMAT DELIMITED 
  FIELDS TERMINATED BY ','
 
-- 데이터 저장 형식 지정 
STORED AS 
INPUTFORMAT 
  'org.apache.hadoop.mapred.TextInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.HiveIgnoreKeyTextOutputFormat'                                                                                  

LOCATION 's3://sydev-lake/data/tripdata'
TBLPROPERTIES ("skip.header.line.count"="1");